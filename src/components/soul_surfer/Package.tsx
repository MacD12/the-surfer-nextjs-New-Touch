'use client';
import CampRatesSection from '@/components/CampRatesSection';

/**
 * Soul Surfer Camp rates — rendered in the same format as the /rates page,
 * driven by the shared `soulSurferPackages` i18n data.
 */
const Package = () => <CampRatesSection ns="soulSurferPackages" />;

export default Package;
